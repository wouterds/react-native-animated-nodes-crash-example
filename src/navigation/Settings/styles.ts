import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  button: {
    marginTop: 12,
    alignItems: 'center',
    backgroundColor: '#0984e3',
    padding: 8,
    borderRadius: 4,
    marginBottom: 16,
  },
  buttonLabel: {
    color: '#fff',
  },
});
